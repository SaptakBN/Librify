import { DollarToInrPipe } from './dollar-to-inr.pipe';

describe('DollarToInrPipe', () => {
  it('create an instance', () => {
    const pipe = new DollarToInrPipe();
    expect(pipe).toBeTruthy();
  });
});
